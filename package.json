{
    "scripts": {
        "dev": "concurrently \"cd backend && python -m uvicorn app:app --reload --port 9000\" \"cd frontend && npm start\" \"cd frontend/electron && npm start\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}
